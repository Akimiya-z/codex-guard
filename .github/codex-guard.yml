# Dogfood: start from the balanced preset, then keep this repo's narrower gate.
# Only secrets and CI block merges; TODO leftovers and sloppy commit subjects
# are reported but non-blocking.
preset: balanced
gate-agents-only: true

fail-on:
  - secrets
  - ci

comment-mode: replace
request-changes: true
