# Dogfood: policy comes from this file (v1.3.0+).
# Only secrets and CI block merges in this repo; TODO leftovers and sloppy
# commit subjects are reported but non-blocking.
gate-agents-only: true

fail-on:
  - secrets
  - ci

comment-mode: replace
request-changes: true
