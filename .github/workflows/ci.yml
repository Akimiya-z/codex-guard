name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    # Codex Guard's job is babysitting agent PRs — its own keep it honest.
    name: test (Node ${{ matrix.node }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: [22, 24]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ matrix.node }}
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run check
      - name: verify checked-in Action bundle
        if: matrix.node == 24
        run: |
          npm run build
          git diff --exit-code -- dist/

  validate-action:
    name: validate YAML manifests
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: parse action.yml
        run: |
          set +e
          npm exec --offline -- action-validator action.yml > /tmp/val.out 2>&1
          code=$?
          [ $code -eq 0 ] && exit 0
          # action-validator lags behind GitHub's current runtimes and rejects
          # the node24 `runs.using` value that GitHub now supports. Tolerate
          # only that specific enum error; everything else still fails.
          if grep -q '/runs/using' /tmp/val.out && grep -q 'Enum conditions are not met' /tmp/val.out; then
            echo "::warning::action-validator is outdated for node24; structural checks skipped"
            exit 0
          fi
          cat /tmp/val.out
          exit 1
      - name: parse workflow files
        run: |
          for f in .github/workflows/*.yml; do
            npm exec --offline -- action-validator "$f" > /tmp/wf.out 2>&1 || {
              cat /tmp/wf.out
              exit 1
            }
          done
          echo "all workflows OK"

  coverage:
    name: coverage report
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm test -- --experimental-test-coverage 2>&1 | tail -20
