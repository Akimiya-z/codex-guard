# GitHub Actions executes the checked-in dist/ bundle. Keep install artifacts
# local so dependency updates remain reviewable.
node_modules/

# Local tooling
npm-debug.log*
*.tgz
.DS_Store
*.log
.coverage/
